# Backend Block Diagrams

**Last Updated:** 2026-01-28
**Purpose:** Visual representations of Selene's backend architecture and data flows

---

## 1. System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        CAPTURE LAYER                            │
│                                                                 │
│  ┌──────────┐                                                  │
│  │  Drafts  │ ──────┐                                          │
│  │   App    │       │                                          │
│  └──────────┘       │  HTTP POST                               │
│                     │  (JSON payload)                          │
│  ┌──────────┐       ↓                                          │
│  │ Shortcuts│  ┌─────────────────┐                             │
│  │   Action │→ │ Fastify Server  │                             │
│  └──────────┘  │  (port 5678)    │                             │
│                └─────────────────┘                              │
│                         ↓                                       │
│                    INSERT INTO                                  │
│                    raw_notes                                    │
└─────────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│                      PROCESSING LAYER                           │
│                   (launchd scheduled jobs)                      │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Every 5 min: process-llm.ts                           │    │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────────┐     │    │
│  │  │  SELECT  │ →  │  Ollama  │ →  │    INSERT    │     │    │
│  │  │ pending  │    │ mistral  │    │  processed_  │     │    │
│  │  │  notes   │    │   :7b    │    │    notes     │     │    │
│  │  └──────────┘    └──────────┘    └──────────────┘     │    │
│  │  Extracts: concepts, themes, energy, sentiment         │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Every 5 min: compute-embeddings.ts                    │    │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────────┐     │    │
│  │  │  SELECT  │ →  │  Ollama  │ →  │    INSERT    │     │    │
│  │  │unindexed │    │  nomic-  │    │note_embeddings│    │    │
│  │  │  notes   │    │  embed   │    │              │     │    │
│  │  └──────────┘    └──────────┘    └──────────────┘     │    │
│  │  Generates: 768-dim vectors for semantic search        │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Every 5 min: index-vectors.ts                         │    │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────────┐     │    │
│  │  │  SELECT  │ →  │ LanceDB  │ →  │    Store     │     │    │
│  │  │unindexed │    │  Index   │    │   vectors    │     │    │
│  │  │embeddings│    │          │    │              │     │    │
│  │  └──────────┘    └──────────┘    └──────────────┘     │    │
│  │  Stores: Vectors in LanceDB for fast retrieval         │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Every 5 min: compute-relationships.ts                 │    │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────────┐     │    │
│  │  │  Query   │ →  │ LanceDB  │ →  │    INSERT    │     │    │
│  │  │ vectors  │    │ Cosine   │    │ note_        │     │    │
│  │  │          │    │similarity│    │relationships │     │    │
│  │  └──────────┘    └──────────┘    └──────────────┘     │    │
│  │  Computes: Similarity scores > threshold (0.65)        │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Every 30 min: detect-threads.ts                       │    │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────────┐     │    │
│  │  │  SELECT  │ →  │  Cluster │ →  │    INSERT    │     │    │
│  │  │relations │    │ Algorithm│    │   threads    │     │    │
│  │  │ships     │    │ + LLM    │    │note_threads  │     │    │
│  │  └──────────┘    └──────────┘    └──────────────┘     │    │
│  │  Creates: Thread clusters (3+ related notes)           │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Every hour: reconsolidate-threads.ts                  │    │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────────┐     │    │
│  │  │  SELECT  │ →  │  Ollama  │ →  │    UPDATE    │     │    │
│  │  │ active   │    │ Generate │    │   threads    │     │    │
│  │  │ threads  │    │ Summary  │    │   (summary,  │     │    │
│  │  └──────────┘    └──────────┘    │   momentum)  │     │    │
│  │                                   └──────────────┘     │    │
│  │  Updates: Summaries, momentum scores                   │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Every hour: export-obsidian.ts                        │    │
│  │  ┌──────────┐    ┌──────────┐    ┌──────────────┐     │    │
│  │  │  SELECT  │ →  │ Generate │ →  │    WRITE     │     │    │
│  │  │unexported│    │ Markdown │    │   .md files  │     │    │
│  │  │ threads  │    │  + YAML  │    │   to vault   │     │    │
│  │  └──────────┘    └──────────┘    └──────────────┘     │    │
│  │  Exports: Thread summaries to Obsidian vault           │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────────┐
│                      RETRIEVAL LAYER                            │
│                                                                 │
│  ┌──────────────┐                  ┌──────────────┐            │
│  │  SeleneChat  │ ← SQLite Query → │   SQLite DB  │            │
│  │  (macOS app) │                  │              │            │
│  └──────────────┘                  └──────────────┘            │
│         ↓                                                       │
│  ┌──────────────┐                                              │
│  │  Query       │  Vector search for semantic queries          │
│  │  Analyzer    │  Direct SQL for thread queries               │
│  └──────────────┘  Keyword search for specific lookups         │
│         ↓                                                       │
│  ┌──────────────┐                  ┌──────────────┐            │
│  │  LLM         │ ← Context Notes ← │  LanceDB API │            │
│  │  Response    │                  │  (if needed) │            │
│  └──────────────┘                  └──────────────┘            │
│         ↓                                                       │
│  Display to user with citations                                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. Query Routing Decision Tree

```
┌─────────────────────────────────────────────────────────┐
│              User types query in SeleneChat             │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│              QueryAnalyzer.analyze(query)               │
└─────────────────────────────────────────────────────────┘
                           ↓
              ┌────────────┴────────────┐
              │                         │
         Thread Query?             Not Thread
              │                         │
              YES                       NO
              ↓                         ↓
    ┌─────────────────┐      ┌──────────────────┐
    │ Pattern Match:  │      │ Should use       │
    │ - "show me X    │      │ semantic search? │
    │    thread"      │      └──────────────────┘
    │ - "what's       │                │
    │    emerging"    │         ┌──────┴──────┐
    └─────────────────┘         │             │
              ↓                YES            NO
    ┌─────────────────┐         ↓             ↓
    │ Direct Database │   ┌──────────┐  ┌──────────┐
    │ Query           │   │ Vector   │  │ Keyword  │
    │ - No LLM needed │   │ Search   │  │ Search   │
    │ - Fast response │   │ LanceDB  │  │ SQLite   │
    └─────────────────┘   └──────────┘  └──────────┘
              ↓                 ↓             ↓
    ┌─────────────────┐   ┌──────────────────────┐
    │ Format thread   │   │ Found notes?         │
    │ details:        │   └──────────────────────┘
    │ - Summary       │         │         │
    │ - Momentum      │        YES        NO
    │ - Linked notes  │         ↓         ↓
    └─────────────────┘   ┌──────────┐  ┌──────────┐
              ↓           │ Build    │  │ "No notes│
              │           │ Context  │  │ matching"│
              │           └──────────┘  └──────────┘
              │                 ↓
              │           ┌──────────┐
              │           │ Ollama   │
              │           │ Generate │
              │           │ Response │
              │           └──────────┘
              │                 ↓
              └─────────────────┴─────────────────┐
                                                  │
                                                  ↓
                                    ┌──────────────────────┐
                                    │ Format with citations│
                                    │ Display to user      │
                                    └──────────────────────┘
```

---

## 3. Thread Detection & Clustering Flow

```
┌──────────────────────────────────────────────────────────────┐
│           detect-threads.ts (runs every 30 min)              │
└──────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  Step 1: Load relationships from database                    │
│  SELECT * FROM note_relationships WHERE similarity > 0.65    │
└──────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  Step 2: Build graph of related notes                        │
│                                                               │
│    Note 45 ──0.72──> Note 48 ──0.68──> Note 52              │
│      │                 │                                      │
│    0.71              0.69                                     │
│      │                 │                                      │
│      └────────────> Note 50                                  │
│                                                               │
│  Each edge = similarity score                                │
└──────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  Step 3: Clustering algorithm (Community Detection)          │
│                                                               │
│  Find densely connected groups (min 3 notes)                 │
│                                                               │
│  Cluster 1: [45, 48, 50, 52]  ← 4 related notes             │
│  Cluster 2: [23, 27, 31]      ← 3 related notes             │
└──────────────────────────────────────────────────────────────┘
                            ↓
┌──────────────────────────────────────────────────────────────┐
│  Step 4: Filter out notes already in threads                 │
│                                                               │
│  Check: note_threads table for existing assignments          │
│  Keep only unthreaded notes in each cluster                  │
└──────────────────────────────────────────────────────────────┘
                            ↓
         ┌──────────────────┴──────────────────┐
         │                                     │
    Cluster < 3                           Cluster ≥ 3
    unthreaded notes                      unthreaded notes
         │                                     │
         ↓                                     ↓
    ┌─────────┐                         ┌──────────────────┐
    │  SKIP   │                         │ Step 5: LLM      │
    │  (too   │                         │ Analysis         │
    │  small) │                         └──────────────────┘
    └─────────┘                                  ↓
                                  ┌──────────────────────────┐
                                  │ Send to Ollama:          │
                                  │ - Note titles            │
                                  │ - Note excerpts          │
                                  │ - Similarity scores      │
                                  │                          │
                                  │ Prompt:                  │
                                  │ "Analyze these related   │
                                  │  notes. What is the      │
                                  │  underlying thread       │
                                  │  connecting them?"       │
                                  └──────────────────────────┘
                                              ↓
                                  ┌──────────────────────────┐
                                  │ LLM Returns:             │
                                  │ - Thread name            │
                                  │ - Summary                │
                                  │ - Why connected          │
                                  └──────────────────────────┘
                                              ↓
                                  ┌──────────────────────────┐
                                  │ Step 6: Create Thread    │
                                  │                          │
                                  │ INSERT INTO threads      │
                                  │   (name, summary, why,   │
                                  │    status, note_count)   │
                                  │                          │
                                  │ INSERT INTO note_threads │
                                  │   (thread_id, note_id)   │
                                  │   for each note in       │
                                  │   cluster                │
                                  └──────────────────────────┘
                                              ↓
                                  ┌──────────────────────────┐
                                  │ New thread created!      │
                                  │ Will be updated hourly   │
                                  │ by reconsolidate-        │
                                  │ threads.ts               │
                                  └──────────────────────────┘
```

---

## 4. Note Lifecycle Timeline

```
T+0 min     User captures note in Drafts
            "I need to research signature scents"
                        ↓
            Drafts "Selene Process" action triggered
                        ↓
            HTTP POST → localhost:5678/webhook/api/drafts
                        ↓
            server.ts receives webhook
                        ↓
            INSERT INTO raw_notes
            {
              id: 121,
              title: "I need to research...",
              content: "Full note text...",
              created_at: "2026-01-28T22:35:01Z",
              content_hash: "abc123..."  ← Duplicate detection
            }
            ✓ Status: Captured
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

T+5 min     process-llm.ts runs (launchd trigger)
                        ↓
            SELECT * FROM raw_notes
            WHERE id NOT IN (
              SELECT raw_note_id FROM processed_notes
            )
            LIMIT 10
                        ↓
            Found: [121]
                        ↓
            Send to Ollama mistral:7b
            Prompt: "Extract concepts, themes, energy..."
                        ↓
            Ollama returns:
            {
              concepts: ["scent", "research", "identity"],
              primary_theme: "self-expression",
              energy_level: "medium",
              ...
            }
                        ↓
            INSERT INTO processed_notes
            {
              raw_note_id: 121,
              concepts: '["scent","research","identity"]',
              primary_theme: "self-expression",
              energy_level: "medium",
              ...
            }
            ✓ Status: Processed
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

T+10 min    compute-embeddings.ts runs
                        ↓
            SELECT * FROM raw_notes
            WHERE id NOT IN (
              SELECT note_id FROM note_embeddings
            )
                        ↓
            Found: [121]
                        ↓
            Send to Ollama nomic-embed-text
            Input: Note title + content
                        ↓
            Ollama returns: [0.23, -0.15, 0.67, ...] ← 768 dims
                        ↓
            INSERT INTO note_embeddings
            {
              note_id: 121,
              embedding: '[0.23,-0.15,0.67,...]'
            }
            ✓ Status: Embedded
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

T+15 min    index-vectors.ts runs
                        ↓
            Connect to LanceDB
            /Users/.../data/vectors.lance
                        ↓
            SELECT embedding FROM note_embeddings
            WHERE indexed = 0
                        ↓
            Found: [121]
                        ↓
            LanceDB.insert({
              id: 121,
              vector: Float32Array([0.23, -0.15, ...])
            })
                        ↓
            UPDATE note_embeddings
            SET indexed = 1 WHERE id = 121
            ✓ Status: Indexed
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

T+20 min    compute-relationships.ts runs
                        ↓
            For note 121:
            Query LanceDB for similar vectors
            (cosine similarity > 0.65)
                        ↓
            LanceDB returns:
            [
              {id: 85, similarity: 0.72},  ← "scent preferences"
              {id: 92, similarity: 0.68}   ← "personal style"
            ]
                        ↓
            INSERT INTO note_relationships VALUES
              (121, 85, 0.72),
              (121, 92, 0.68)
            ✓ Status: Related
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

T+30 min    detect-threads.ts runs
                        ↓
            SELECT * FROM note_relationships
            WHERE similarity > 0.65
                        ↓
            Build graph, find clusters
                        ↓
            Found cluster: [85, 92, 121] ← 3 notes, new!
                        ↓
            Not already threaded? ✓
                        ↓
            Send to Ollama for thread analysis
            Prompt: "What connects these notes?"
                        ↓
            Ollama returns:
            {
              name: "Personal Identity & Expression",
              summary: "Exploring authentic self...",
              why: "All relate to finding and expressing..."
            }
                        ↓
            INSERT INTO threads
            {
              id: 15,
              name: "Personal Identity & Expression",
              summary: "Exploring authentic...",
              status: "active",
              note_count: 3,
              momentum_score: 1.2  ← Initial calc
            }
                        ↓
            INSERT INTO note_threads VALUES
              (15, 85), (15, 92), (15, 121)
            ✓ Status: Threaded
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

T+60 min    reconsolidate-threads.ts runs
                        ↓
            SELECT * FROM threads WHERE status = 'active'
                        ↓
            Found: [Thread 15] (plus others)
                        ↓
            Get all notes in thread:
            SELECT notes WHERE id IN (
              SELECT raw_note_id FROM note_threads
              WHERE thread_id = 15
            )
                        ↓
            Send updated context to Ollama
            Prompt: "Regenerate summary with all notes..."
                        ↓
            Ollama returns updated summary
                        ↓
            Calculate momentum:
            - Notes added in last 7 days: 1
            - Notes added in last 30 days: 3
            - Momentum score: 1.5 (increasing!)
                        ↓
            UPDATE threads SET
              summary = "...",
              momentum_score = 1.5
            WHERE id = 15
            ✓ Status: Reconsolidated
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

T+60 min    export-obsidian.ts runs
            (same hour)
                        ↓
            SELECT * FROM threads
            WHERE needs_export = 1
                        ↓
            Found: [Thread 15]
                        ↓
            Generate markdown:
            ```markdown
            ---
            thread_id: 15
            name: Personal Identity & Expression
            momentum: 1.5
            note_count: 3
            ---
            # Personal Identity & Expression

            Summary: Exploring authentic...

            ## Linked Notes
            - [[I need to research signature scents]]
            - [[Personal style evolution]]
            - [[Scent preferences]]
            ```
                        ↓
            WRITE to /vault/Threads/personal-identity.md
                        ↓
            UPDATE threads SET needs_export = 0
            ✓ Status: Exported
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

T+forever   User opens SeleneChat
                        ↓
            "show me personal identity thread"
                        ↓
            QueryAnalyzer detects thread query
                        ↓
            SELECT * FROM threads WHERE name LIKE '%personal identity%'
            JOIN note_threads ON threads.id = thread_id
            JOIN raw_notes ON note_threads.raw_note_id = raw_notes.id
                        ↓
            Format response with thread details
                        ↓
            Display to user:

            "**Personal Identity & Expression**

            Summary: Exploring authentic self...
            Momentum: 1.5 🔥

            Linked Notes (3):
            - [1] I need to research signature scents
            - [2] Personal style evolution
            - [3] Scent preferences"

            ✓ User can click notes to view details
            ✓ User can discuss and refine thread
            ✓ New insights captured → adds to thread!
```

---

## 5. Data Model Relationships

```
┌─────────────────┐
│   raw_notes     │  ← Original captured content
│─────────────────│
│ id (PK)         │
│ title           │
│ content         │
│ created_at      │
│ content_hash    │  ← Duplicate detection
│ test_run        │  ← Marks test data
└─────────────────┘
         │
         │ 1:1
         ↓
┌─────────────────┐
│processed_notes  │  ← LLM extracted metadata
│─────────────────│
│ id (PK)         │
│ raw_note_id (FK)│ ─────────┐
│ concepts        │           │
│ primary_theme   │           │
│ energy_level    │           │
│ sentiment       │           │
└─────────────────┘           │
         │                    │
         │ 1:1                │
         ↓                    │
┌─────────────────┐           │
│note_embeddings  │  ← Semantic vectors
│─────────────────│           │
│ id (PK)         │           │
│ note_id (FK)    │ ──────────┤
│ embedding       │  (768 dim)│
│ indexed         │           │
└─────────────────┘           │
         │                    │
         │ vector stored      │
         ↓                    │
┌─────────────────┐           │
│ LanceDB Index   │  ← Fast retrieval
│─────────────────│           │
│ id              │           │
│ vector          │           │
└─────────────────┘           │
         │                    │
         │ similarity         │
         ↓                    │
┌─────────────────────────┐  │
│ note_relationships      │  │ ← Note-to-note links
│─────────────────────────│  │
│ id (PK)                 │  │
│ note_id_1 (FK)          │ ─┤
│ note_id_2 (FK)          │ ─┤
│ similarity_score        │  │
│ relationship_type       │  │
└─────────────────────────┘  │
         │                   │
         │ M:N (clustered)   │
         ↓                   │
┌─────────────────┐          │
│    threads      │  ← Clustered groups
│─────────────────│          │
│ id (PK)         │          │
│ name            │          │
│ summary         │          │
│ why             │          │
│ status          │          │
│ note_count      │          │
│ momentum_score  │          │
└─────────────────┘          │
         │                   │
         │ M:N               │
         ↓                   │
┌─────────────────┐          │
│  note_threads   │  ← Link table
│─────────────────│          │
│ thread_id (FK)  │ ─────────┤
│ raw_note_id (FK)│ ─────────┘
│ added_at        │
└─────────────────┘
```

---

## 6. Privacy & LLM Routing (SeleneChat)

```
┌──────────────────────────────────────────────────────────┐
│            User query in SeleneChat                      │
└──────────────────────────────────────────────────────────┘
                         ↓
┌──────────────────────────────────────────────────────────┐
│         PrivacyRouter.routeQuery(query, notes)           │
└──────────────────────────────────────────────────────────┘
                         ↓
        ┌────────────────┴────────────────┐
        │                                 │
   Contains PII?                    No PII detected
   (names, dates,                         │
    sensitive topics)                     │
        │                                 │
        YES                               NO
        ↓                                 ↓
┌───────────────┐              ┌──────────────────┐
│  Tier: Local  │              │ Planning/        │
│               │              │ technical query? │
│  Route to:    │              └──────────────────┘
│  Ollama       │                      │
│  (mistral:7b) │               ┌──────┴──────┐
│               │               │             │
│  Privacy:     │              YES            NO
│  ✓ All local  │               ↓             ↓
│  ✓ No network │    ┌─────────────┐  ┌─────────────┐
│  ✓ Full data  │    │ Tier:       │  │ Tier:       │
└───────────────┘    │ External    │  │ On-Device   │
        │            │             │  │             │
        │            │ Route to:   │  │ Route to:   │
        │            │ (Future)    │  │ (Future)    │
        │            │ Claude API  │  │ Apple       │
        │            │             │  │ Intelligence│
        │            │ Privacy:    │  │             │
        │            │ ✓ Sanitized │  │ Privacy:    │
        │            │ ✓ No PII    │  │ ✓ E2E       │
        │            │ ✓ Generic   │  │   encrypted │
        │            │   context   │  │ ✓ No Apple  │
        │            └─────────────┘  │   retention │
        │                   │         └─────────────┘
        │                   │                │
        └───────────────────┴────────────────┘
                            ↓
              ┌──────────────────────────┐
              │ Generate response with   │
              │ citations                │
              └──────────────────────────┘
                            ↓
              ┌──────────────────────────┐
              │ Display tier badge:      │
              │ 🟣 Local                 │
              │ 🟢 On-Device             │
              │ 🟡 External              │
              └──────────────────────────┘
```

---

*These diagrams complement `backend-theory-of-operation.md` and provide visual references for understanding Selene's architecture.*
