#!/bin/bash
# Post-merge hook: Auto-build SeleneChat when its files change

set -e

SCRIPT_DIR="$(dirname "$0")"
# Handle symlinked hooks
if [ -L "$0" ]; then
    SCRIPT_DIR="$(dirname "$(readlink "$0")")"
fi

"$SCRIPT_DIR/selenechat-auto-build.sh" "ORIG_HEAD" "HEAD"
